{{#> layout}}
  <section class="py-8">
    <h2 class="text-2xl font-semibold mb-6">Contacts</h2>
    <div hx-get="/contact" hx-trigger="load" hx-swap="inner" class="px-4"></div>
  </section>
  {{> conditionalView}}
{{/layout}}
